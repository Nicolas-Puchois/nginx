#  Environement dev
services:
  monnginx:
    build: ./front
    ports:
      - "8000:80"
    volumes:
      - ./front/app/:/usr/share/nginx/html
    environment:
      - MYSQL_PASSWORD=${PASSWORD}
      # volumes:
      #   - type: volume
      #     #- type: bind
      #     # source ./front/app
      #     source: mes_datas
      #     target: /usr/share/nginx/html

      # volumes:
      #   mes_datas:
